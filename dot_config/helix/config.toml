theme = "nord_transparent"

[editor]
line-number = "relative"
bufferline = "multiple"
gutters = ["line-numbers", "spacer", "diff"]
scrolloff = 10
color-modes = true
true-color = true
undercurl = true
soft-wrap.enable = true
rulers = [120]
clipboard-provider = "wayland"
end-of-line-diagnostics = "hint"
file-picker.hidden = false
path-completion = true
auto-format = true
trim-trailing-whitespace = true
trim-final-newlines = true

[editor.indent-guides]
render = truetrim-trailing-whitespace 
character = "▏" 

[editor.cursor-shape]
normal = "block"
insert = "bar"
select = "underline"

[editor.inline-diagnostics]
cursor-line = "hint"

[keys.select]
# Vim-style paragraph motions
"{" = "goto_prev_paragraph"
"}" = "goto_next_paragraph"

[editor.statusline]
mode.normal = "NORMAL"
mode.insert = "INSERT"
mode.select = "SELECT"
left = [
  "mode",
  "file-name",
  "version-control",
  "spinner",
  "read-only-indicator",
  "file-modification-indicator",
]
right = ["diagnostics", "selections", "position", "file-type"]
separator = '-'

[editor.lsp]
goto-reference-include-declaration = false
display-inlay-hints = false
display-progress-messages = true

[editor.whitespace.characters]
space = "·"
nbsp = "⍽"
nnbsp = "␣"
tab = "→"
newline = "⏎"
tabpad = "␣" 

[editor.whitespace]
render = "all"

[keys.normal]
H = ":buffer-previous"
L = ":buffer-next"
G = "goto_file_end"
V = ["extend_line_below", "select_mode"]
y = "yank_to_clipboard"
Y = "yank"

[editor.auto-pairs]
'(' = ')'
'{' = '}'
'[' = ']'
'"' = '"'
'`' = '`'
'<' = '>'

[keys.normal.space]
q = ":quit"
e = [
  ':sh rm -f /tmp/yazi-path',
  ':insert-output yazi %{buffer_name} --chooser-file=/tmp/yazi-path',
  ":open %sh{cat /tmp/yazi-path}",
  ":redraw",
]

[keys.normal.space.b]
"d" = ":buffer-close"

[keys.normal.space.g]
c = "toggle_comments"  

[keys.normal.space.s]
h = ":sh hx --help"  
f = "file_picker_in_current_directory"  # [S]earch [F]iles
w = "search_selection"  # [S]earch current [W]ord 
g = "global_search"  # [S]earch by [G]rep
d = "diagnostics_picker"  # [S]earch [D]iagnostics
b = "buffer_picker"  # [S]earch [B]uffers

[keys.insert]
C-h = ["jump_view_left", "insert_mode"]
C-l = ["jump_view_right", "insert_mode"]
